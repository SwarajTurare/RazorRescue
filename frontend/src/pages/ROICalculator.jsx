import { useEffect, useState } from 'react'; import { IndianRupee, TrendingUp } from 'lucide-react'; import { api } from '../services/api'; import { toast } from 'react-toastify'; import SectionHeader from '../components/ui/SectionHeader'; import MetricCard from '../components/ui/MetricCard';
export default function ROICalculator(){const [gmv,setGmv]=useState(5000000),[drop,setDrop]=useState(15),[eff,setEff]=useState(34),[out,setOut]=useState(null);useEffect(()=>{api.roi({gmv,dropPct:drop,recoveryPct:eff}).then(setOut).catch(e=>toast.error(e.message))},[gmv,drop,eff]);return <div><SectionHeader eyebrow="Business case" title="Merchant revenue impact" description="Model the financial value of bounded recovery automation from monthly GMV, payment drop-off, and recovery efficiency."/><div className="grid gap-4 lg:grid-cols-[1fr_1fr]"><section className="rr-surface rounded-2xl p-5"><Slider label="Monthly GMV" value={gmv} min={500000} max={50000000} step={500000} onChange={setGmv} formatter={v=>`₹${v.toLocaleString('en-IN')}`}/><Slider label="Payment drop-off rate" value={drop} min={5} max={30} step={1} onChange={setDrop} formatter={v=>`${v}%`}/><Slider label="Projected recovery efficiency" value={eff} min={15} max={50} step={1} onChange={setEff} formatter={v=>`${v}%`}/></section><section className="grid gap-3 sm:grid-cols-2"><MetricCard label="Monthly revenue recovered" value={`₹${(out?.recoveredMonthly||0).toLocaleString('en-IN')}`} icon={TrendingUp}/><MetricCard label="Annual revenue recovered" value={`₹${(out?.annualRecovered||0).toLocaleString('en-IN')}`} icon={IndianRupee}/><MetricCard label="Annual revenue at risk" value={`₹${(out?.annualAtRisk||0).toLocaleString('en-IN')}`} caption="Before recovery"/><MetricCard label="Recovery yield" value={`${eff}%`} caption="Scenario input"/></section></div></div>}
function Slider({label,value,min,max,step,onChange,formatter}){return <label className="mb-7 block"><div className="flex items-center justify-between text-sm"><span>{label}</span><span className="text-[#45B5E7]">{formatter(value)}</span></div><input className="mt-4 w-full accent-[#45B5E7]" type="range" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/><div className="mt-2 flex justify-between text-[10px] text-white/25"><span>{formatter(min)}</span><span>{formatter(max)}</span></div></label>}
